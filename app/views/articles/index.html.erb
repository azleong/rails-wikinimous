<nav class="navbar navbar-dark bg-primary">
  <a class="navbar-brand" href="#">Andy and Henry's Wikinimous</a>
  <%= link_to '<i class="fas fa-plus"></i>'.html_safe, new_article_path, class: "btn btn-primary" %>
</nav>

<ol>
  <% @articles.each do |article| %>
  <li>
    <%= link_to article.title, article_path(article.title) %>: <p><%= article.content %></p> <hr> 
    </li>
  <% end %>
</ol>